# Exclude dev and source files from the VSIX

# VCS / editor / CI
.vscode/**
.git/**
.github/**
.gitignore

# Sources and configs not needed at runtime
src/**
**/*.ts
**/*.tsx
**/*.map
tsconfig.json
.eslintrc*
.prettier*
.npmrc

# Build artifacts not needed inside the VSIX
coverage/**
dist/**

# Node deps (we bundle runtime deps into out/extension.js). Do not ship CLI or binaries.
node_modules/**

# Misc
.DS_Store
bun.lock*


